// Mirrors Laravel's Str::slug for a live preview; the backend generates the real slug.
export function slugify(text: string) {
  return text
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "");
}
